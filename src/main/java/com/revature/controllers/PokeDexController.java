package com.revature.controllers;

public class PokeDexController {
	
/*	public Handler "insert name here" = ctx ->{
        if(ctx.req.getSession(true) != null) {
            try{ 
                List<"insert object"> "insert object" = "insert object".get"insert object"();
                Gson gson = new Gson();
                String JSON"insert object" = gson.toJson("insert object");
                ctx.result(JSON"insert object");
                ctx.status(200);
            }
            catch(Exception e) {
                ctx.result("There was an error while fetching insert object Requests");
                ctx.status(404);
            }
        }
        else {
            ctx.result("Unauthorized User");
            ctx.status(403);    
        }

    };*/
}
